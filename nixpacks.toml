[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "cd frontend && npm ci",
  "cd backend && npm ci --production"
]

[phases.build]
cmds = ["cd frontend && npm run build"]

[start]
cmd = "node backend/src/index.js"
